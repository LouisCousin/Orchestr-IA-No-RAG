# Orchestr'IA - Configuration des clés API
# Copiez ce fichier en .env et remplissez vos clés

# OpenAI (Phase 1)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic (Phase 2)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# Google Gemini (Phase 2)
# GOOGLE_API_KEY=your-google-api-key-here

# Token GitHub (Phase 6 — optionnel mais recommandé pour éviter le rate limit à 60 req/h)
# Droits requis : Contents (read-only) pour les dépôts publics
# Droits requis : Contents (read-only) + repo pour les dépôts privés
# Création : GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens
# GITHUB_TOKEN=ghp_...
